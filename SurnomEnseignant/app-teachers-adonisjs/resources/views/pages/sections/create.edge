@component('components/layout', { title: 'Ajout section' })
  <form action="{{ route('sections.create') }}" method="POST">
    {{ csrfField() }}
    <div class="field">
      <label for="name">Nom</label>
      <input type="text" id="name" name="name" value="{{ old('name') || '' }}" />
      @inputError('name')
        <p class="text-danger">
          {{ $messages.join(', ') }}
        </p>
      @end
    </div>
    <button type="submit">Ajouter</button>
  </form>
@endcomponent
